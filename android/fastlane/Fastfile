default_platform(:android)

platform :android do
  desc "Build APK for given flavor"
  lane :build do
    gradle(
      task: "assemble",
      build_type: "Release",
      flavor: ENV["FLAVOR"]
    )
    # No distribution/upload actions - just build
  end
end